import React from 'react'

const single = () => {

//     const { id } = useParams();
//   const [blog, setBlog] = useState(null);

//   useEffect(() => {
//     let blog = items.find(blog => blog.id === parseInt(id))

//     if (blog) {
//       setBlog(blog)
//     }
//   }, [id])
  return (
    <div>
      
    </div>
  )
}

export default single
